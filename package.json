{
  "name": "breast-cancer-predictor",
  "version": "1.0.0",
  "description": "API para predicción de cáncer de mama",
  "main": "app.js",
  "engines": {
    "node": ">=14.0.0",
    "python": "3.11.0"
  },
  "scripts": {
    "start": "node app.js",
    "postinstall": "apt-get update && apt-get install -y python3-pip python3-distutils python3-setuptools && pip3 install -r requirements.txt && npm install"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.17.1",
    "mysql2": "^3.11.4",
    "prisma": "^5.22.0"
  }
}
